'use strict'
/*
* Algoritmos para convertir litros en centilitros
* 1. Inicio del algoritmo
* 2. Leer la cantidad de litros (litros)
* 3. Calcular la cantidad equivalente en centilitros: centilitros = litros * 100
* 4. Mostrar el resultado: "La cantidad de", litros, "litros es equivalente a", centilitros, "centilitros."
* 5. Fin del algoritmo
*/

let cantidadLitros = 5;
const cantidadCentilitros = cantidadLitros * 100;
console.log(cantidadLitros+" litros es equivalente a, "+cantidadCentilitros+" centilitros");

/* Algoritmos para convertir litros en decilitros
* 1. Inicio del algoritmo
* 2. Leer la cantidad de litros (litros)
* 3. Calcular la cantidad equivalente en decilitros: decilitros = litros * 10
* 4. Mostrar el resultado: "La cantidad de", litros, "litros es equivalente a", decilitros, "decilitros."
* 5. Fin del algoritmo
*/

const cantidadDecilitros = cantidadLitros * 10;
console.log(cantidadLitros+" litros es equivalente a, "+cantidadDecilitros+" decilitros");

/*
* Algoritmos para convertir litros en galones
* 1. Inicio del algoritmo
* 2. Leer la cantidad de litros (litros)
* 3. Calcular la cantidad equivalente en galones: galones = litros * 0.264172
* 4. Mostrar el resultado: "La cantidad de", litros, "litros es equivalente a", galones, "galones."
* 5. Fin del algoritmo
*/

const cantidadGalones = cantidadLitros * 0.264172;
console.log(cantidadLitros+" litros es equivalente a, "+cantidadGalones+" galones");

/*
* Algoritmos para convertir litros en onzas
* 1. Inicio del algoritmo
* 2. Leer la cantidad de litros (litros)
* 3. Calcular la cantidad equivalente en onzas: onzas = litros * 33.814
* 4. Mostrar el resultado: "La cantidad de", litros, "litros es equivalente a", onzas, "onzas."
* 5. Fin del algoritmo
*/

const cantidadOnzas = cantidadLitros * 33.814;
console.log(cantidadLitros+" litros es equivalente a, "+cantidadOnzas+" onzas");

